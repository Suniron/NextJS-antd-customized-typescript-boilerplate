/**
 * Put Effects here
 */

export const apiEffects = (() => {
  return {
    initialize() {},
    async isApiOnline() {
      // try {
      //   await fetch(api.URL);
      //   return true;
      // } catch (error) {
      //   return false;
      // }
    },
  };
})();
